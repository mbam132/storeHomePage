export interface IProduct {
  title: string;
  rating: number;
  price: number;
}

export interface IOption {
  title: string;
  url?: string;
}

export interface IService {
  title: string;
  description: string;
}
